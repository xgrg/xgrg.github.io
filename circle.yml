machine:
  python:
    version: 3.3.3
general:
  branches:
    ignore:
      - master

dependencies:
  pre:
     #- pip install --upgrade setuptools
     - pip install pelican markdown ghp-import

test:
   override:
      - "true"

deployment:
  production:
     branch: source
     commands:
       - ./build.sh
