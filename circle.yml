machine:
  python:
    version: 2.7.5
general:
  branches:
    ignore:
      - master

dependencies:
  pre:
     - pip install pelican markdown ghp-import

test:
   override:
      - "true"

deployment:
  production:
     branch: source
     commands:
       - ./build.sh
